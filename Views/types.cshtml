<h1>All Types</h1>
@if (@Model.Count == 0)
{
  <p>There are no types of animals yet.</p>
}
<ul>
  @foreach (var type in Model)
  {
    <li><a href="/types/@type.GetId()">@type.GetType()</a></li>
  }
</ul>
<a href="/types/new">Add new type</a>
